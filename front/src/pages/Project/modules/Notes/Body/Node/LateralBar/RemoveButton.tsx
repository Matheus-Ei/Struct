// Local
import Icon from "components/Icon";

const RemoveButton = () => {
    return (
        <Icon
            value={{ name: "IoRemove", library: "io5" }}
            className="text-2xl"
        />
    );
};

export default RemoveButton;
