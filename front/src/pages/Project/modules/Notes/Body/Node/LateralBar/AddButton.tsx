// Local
import Icon from "components/Icon";

const AddButton = () => {
    return (
        <Icon
            value={{ name: "IoAdd", library: "io5" }}
            className="text-2xl"
        />
    );
};

export default AddButton;
