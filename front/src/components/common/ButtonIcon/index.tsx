// Modules
import * as T from "./types";
import * as S from "./styles";

// Libraries
import React from "react";

const ButtonIcon = ({}: T.ButtonIconProps) => {
    return <S.Body></S.Body>;
};

export default React.memo(ButtonIcon);
